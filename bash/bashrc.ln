[[ "$BASHRC_ALREADY_PROCESSED" == "$$" ]] && return
BASHRC_ALREADY_PROCESSED=$$

for F in $(find -L ~/.dotfiles -name \*.bash); do source $F; done

set -o vi
export EDITOR=vim
export PATH="$HOME/bin:$PATH"
export HISTFILE="$HOME/.history/bash"

